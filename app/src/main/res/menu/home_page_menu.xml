<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/home_navigation"
        android:icon="@drawable/ic_home"
        android:title="@string/menu_main_home_title"
        android:enabled="true"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/routefinder_navigation"
        android:icon="@drawable/ic_location"
        android:title="@string/menu_main_location_title"
        android:enabled="true"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/finance_navigation"
        android:icon="@drawable/ic_finance"
        android:title="@string/menu_main_finance_title"
        android:enabled="true"
        app:showAsAction="ifRoom"/>
</menu>